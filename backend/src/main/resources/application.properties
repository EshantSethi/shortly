# ── Database ────────────────────────────────────────────────────────────────────
# PRODUCTION: set DATABASE_URL to the full JDBC URL from Neon dashboard
# e.g. jdbc:postgresql://ep-xxx.us-east-1.aws.neon.tech/dbname?sslmode=require
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/urlshortener}
spring.datasource.username=${PGUSER:${DB_USERNAME:postgres}}
spring.datasource.password=${PGPASSWORD:${DB_PASSWORD:postgres}}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ── Redis ───────────────────────────────────────────────────────────────────────
spring.data.redis.host=${REDISHOST:${REDIS_HOST:localhost}}
spring.data.redis.port=${REDISPORT:${REDIS_PORT:6379}}
spring.data.redis.password=${REDISPASSWORD:}
spring.data.redis.ssl.enabled=${REDIS_SSL:false}

# ── Server ──────────────────────────────────────────────────────────────────────
server.port=${PORT:8080}
server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=30s

# ── App config ──────────────────────────────────────────────────────────────────
# PRODUCTION: set all three of these env vars
app.base-url=${APP_BASE_URL:http://localhost:8080}
app.frontend-url=${FRONTEND_URL:http://localhost:3000}
# PRODUCTION: set ALLOWED_ORIGINS to your exact frontend domain, e.g. https://shortly.yourdomain.com
app.allowed-origins=${ALLOWED_ORIGINS:*}
app.rate-limit.requests-per-minute=${RATE_LIMIT_RPM:20}

# ── Logging ─────────────────────────────────────────────────────────────────────
logging.level.com.urlshortener=INFO
